services:
  kartappfe:
    container_name: frontend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 5173:80
    networks:
      - kartapp-network

networks:
  kartapp-network:
